import React from 'react';

const Menu = () => {
    return (
        <div className="h-screen flex flex justify-center items-center bg-green-300">
            <h1 className="text-3xl uppercase font-black">Menu Page</h1>
        </div>

    );
};

export default Menu;